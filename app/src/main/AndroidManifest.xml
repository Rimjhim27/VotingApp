<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.votingapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Video"></activity>
        <activity android:name=".PoliticalParty" />
        <activity android:name=".ViewCandidates" />
        <activity android:name=".Choose" />
        <activity android:name=".LoginOrRegister" />
        <activity android:name=".Candidates" />
        <activity android:name=".MainActivity" />
        <activity android:name=".result" />
        <activity android:name=".vote_or_result" />
        <activity android:name=".votingpage" />
        <activity android:name=".biometric_verification" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>